<Window x:Class="MediaMetadataEditor.Views.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Entrada" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <StackPanel Margin="12">
        <TextBlock x:Name="LblMessage" Text="Mensagem" Margin="0,0,0,8"/>
        <TextBox x:Name="TxtInput" Width="420" Margin="0,0,0,8"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="84" Margin="4" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Cancelar" Width="84" Margin="4" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
